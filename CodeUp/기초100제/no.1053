#include <stdio.h>

int main() {
    /* 논리 연산자 ! , && , || */
    // C언어에서 0은 거짓, 0이 아닌 모든 수는 참
    // !0(!거짓) = 1(참)
    // !1(!참) = 0(거짓)
    
    int a;
    scanf("%d",&a);
    
    if(!a) printf("%d",1); // 입력된 값이 0이면 1 출력
    else printf("%d",0); // 입력된 값이 1이면 0 출력
}
