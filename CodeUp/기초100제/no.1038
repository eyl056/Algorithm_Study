#include <stdio.h>

/* 정수 2개 입력받아 합 출력하기1 */

int main() {
    // 입력되는 정수는 -1073741824 ~ 1073741824 // int 범위 넘어가는지 확인 필요
    long long int a, b; // 정수 2개
    scanf("%lld %lld",&a,&b); // 띄어쓰기(공백) 구분으로 입력
    printf("%lld",a+b); // 두 정수의 합 출력
}
