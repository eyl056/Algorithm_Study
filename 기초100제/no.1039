#include <stdio.h>

/* 정수 2개 입력받아 합 출력하기 */

int main() {
    // 입력되는 정수는 -2147483648 ~ +2147483648 = int 형
    long long int a, b; // 정수 2개
    scanf("%lld %lld",&a,&b); // 띄어쓰기(공백) 구분으로 입력
    long long int sum = a+b;
    printf("%lld",sum); // 두 정수의 합 출력
}
