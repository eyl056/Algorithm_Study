#include <stdio.h>

/* 정수 1개 입력받아 그대로 출력하기 */

int main() {
    // -2147483648 ~ +2147483647 범위의 정수 => int n; scanf("%d",&n);
    // 0 ~ 4294967295 범위의 정수 => int n; scanf("%u",&n);

    unsigned int n;
    scanf("%u", &n);
    printf("%u", n);
}
