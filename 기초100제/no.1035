#include <stdio.h>

/* 16진 정수 1개 입력받아 8진수로 출력하기 */

int main() {
    // %o (8진수 형태) : 한 자리에 8개의 문자 사용
    // %x (16진수 형태) 소문자 : 한 자리에 16개의 문자 사용
    
    int num;
    scanf("%x",&num); // 16진수 영문 소문자 형태로 입력
    printf("%o",num); // 8진수 형태로 출력
    
    // (C언어에서 소스 코드 작성 시 0으로 시작하는 수는 8진수로 인식된다.
}
